  // Pour éviter la duplication, on peut extraire l'interface "CardData"
export interface CardData {
    title: string
    imageUrl: string
    imagesCardDetails: string[]
    buttonText: string
    description: string
    tags: string[]
}